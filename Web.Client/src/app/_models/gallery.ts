export class Gallery{
    id: number;
    name: string;
    picId: number

    constructor(id: number, name: string, picId: number) {
        this.id = id,
        this.name = name,
        this.picId = picId
    }
}